// Frontend Configuration
// This file will be used to inject environment variables into the frontend

// Set the API base URL based on environment
window.API_BASE_URL = 'https://your-backend-app-name.onrender.com';

// You can also set other configuration values here
window.APP_CONFIG = {
  environment: 'production',
  version: '1.0.0',
  debug: false
};
